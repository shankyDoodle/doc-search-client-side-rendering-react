# Doc Search App Client Side rendering in React.

Demo:
http://3.16.149.228:1238/index.html
This app is running on AWS with using MongoDB and Web server installed in other repository(doc-search-web-server)

Setup:
* Clone or download repository.
* run 'npm install'.
* run 'npm start' command to initiate parcel

Pre-requisite:
a running web server (in this case running doc-search-web-serve from other repository).
